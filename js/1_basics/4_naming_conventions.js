/**
 * Naming Conventions
 * 
 * // 아래는 지키지 않으면 에러 발생
 * 1) 일반적으로 영어, 문자, 숫자를 모두 사용 가능
 * 2) 특수기호는 언더스코어(_)와 달러($)만 사용 가능
 * 3) 숫자로 이름을 시작할 수 없다.
 * 4) 키워드는 변수명으로 사용할 수 없다.
 * 
 * // 아래는 일종의 약속
 * 5) cameCase, snake_case, PascalCase
 */

let codefactory = '코드팩토리';
var $ive = '아이브';
const _yujin = '안유진';

console.log(codefactory);
console.log($ive);
console.log(_yujin);